/**
 * These functions transform values
 */

import type { CheerioAPI } from "cheerio";
import { readFile } from "fs/promises"

type Transforms = "text-content" | "image-binary"

export default function transform(input: Transforms) {
    
}